version: "3.9"

services:
  flask-app:
    build: ..
    container_name: flask-container
    ports:
      - "5000:5001"
    volumes:
      - ..:/app
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    command: flask run --host=0.0.0.0 --port=5001 --reload
